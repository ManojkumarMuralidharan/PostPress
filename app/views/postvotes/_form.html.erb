<%= form_for(@postvote) do |f| %>
  <% if @postvote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@postvote.errors.count, "error") %> prohibited this postvote from being saved:</h2>

      <ul>
      <% @postvote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
